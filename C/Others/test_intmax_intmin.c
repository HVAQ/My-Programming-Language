#include <stdio.h>
#include <limits.h>

int main() {

    printf ("At my computer INT_MAX is %d\n", INT_MAX);
    printf ("At my computer INT_MIN is %d\n", INT_MIN);
    printf ("At my computer SHRT_MAX is %d\n", SHRT_MAX);
    printf ("At my computer SHRT_MIN is %d\n", SHRT_MIN);
    printf ("At my computer LONG_MAX is %ld\n", LONG_MAX);
    printf ("At my computer LONG_MIN is %ld\n", LONG_MIN);

    return 0;
}
